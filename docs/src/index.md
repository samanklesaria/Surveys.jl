# Surveys.jl: Methods for Design Based Inference

## Simple Random Sampling

```julia
@select(df, :total=π_sum(:robberies, num_counties))
```

Make the examples use datasets from the book.

```@autodocs
Modules = [Surveys]
```
